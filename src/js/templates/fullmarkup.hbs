{{#each this}}
  <img src='{{flags.svg}}' alt='' width='30px' /><h2>{{name.official}}</h2>
  <ul>
    <li><p>{{capital}}</p></li>
    <li><p>{{population}}</p></li>
    <li>{{#each languages }} {{.}}{{#unless @last}},{{/unless}} {{/each}}</li>
  </ul>
{{/each}}